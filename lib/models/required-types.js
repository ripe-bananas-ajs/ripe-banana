const RequiredString = {
  type: String,
  required: true
};

const RequiredDate = {
  type: Date,
  required: true
};

const RequiredNumber = {
  type: Number,
  required: true
};

module.exports = {
  RequiredString,
  RequiredDate,
  RequiredNumber,
};